class Solution():
    def strStr( haystack, needle):
        if haystack and needle:
            idx = haystack.find(needle)
            print(idx)
        return -1

a = Solution.strStr(haystack = "sadbutsad", needle = "l")